eureka:
  client:
    enabled: false

spring:
  application:
    name: chart-service
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
    config:
      enabled: false
